import { StudentDashboardContent } from "@/components/student/StudentDashboardContent";

/** 수강 강좌 — 내 강의실과 동일 목록 (리다이렉트 없이 바로 표시) */
export default function StudentCoursesPage() {
  return <StudentDashboardContent />;
}
